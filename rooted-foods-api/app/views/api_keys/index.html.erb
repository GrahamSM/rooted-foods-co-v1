<p id="notice"><%= notice %></p>

<h1>Listing Api Keys</h1>

<table>
  <thead>
    <tr>
      <th>Access token</th>
      <th>Expires at</th>
      <th>Last access</th>
      <th>User</th>
      <th>Created at</th>
      <th>Updated at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @api_keys.each do |api_key| %>
      <tr>
        <td><%= api_key.access_token %></td>
        <td><%= api_key.expires_at %></td>
        <td><%= api_key.last_access %></td>
        <td><%= api_key.user_id %></td>
        <td><%= api_key.created_at %></td>
        <td><%= api_key.updated_at %></td>
        <td><%= link_to 'Show', api_key %></td>
        <td><%= link_to 'Edit', edit_api_key_path(api_key) %></td>
        <td><%= link_to 'Destroy', api_key, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Api key', new_api_key_path %>
